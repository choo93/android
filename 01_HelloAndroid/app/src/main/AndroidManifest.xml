<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.kh.a01_helloandroid">

    <!--특정한 기능을 사용하는 것들에는 추가적으로 권한이 필요함
        그렇기 때문에 AndroidManifest.xml 파일에 권한 설정이 필요함
    -->
    <uses-permission android:name="android.permission.VIBRATE"/><!--진동 사용 권한-->
    <uses-permission android:name="android.permission.CALL_PHONE"/><!--전화 사용 권한-->
    <uses-permission android:name="android.permission.SEND_SMS"/><!--문자 전송 권한-->

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>